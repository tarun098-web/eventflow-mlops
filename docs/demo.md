# Demo Proof

Open `demo/index.html` in a browser to see a static workflow run dashboard.

The demo shows:

- Pipeline step timeline.
- Job state.
- Step attempt history.
- Final risk scoring output.

Use `sample_outputs/risk_score_job.json` as proof of the job status shape exposed by the API.

